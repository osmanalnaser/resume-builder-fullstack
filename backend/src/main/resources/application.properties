# Mongodb configuration
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI}

# Email configuration
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=465
spring.mail.username=a3c4a3001@smtp-brevo.com
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.starttls.enable=true
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.from=osman.n2@icloud.com

# Application info
app.base.url=http://localhost:8080

# Cloudinary information
cloudinary.cloud-name=dlsytszzz
cloudinary.api-key=${CLOUD_KEY}
cloudinary.api-secret=${CLOUD_SECRET}

# JWT Configuration
jwt.secret=${JWT_SECRET}
jwt.expiration=604800000

# PayPal Developer configuration
paypal.mode=sandbox
paypal.client-id=${PAYPAL_CLIENT_ID}
paypal.client-secret=${PAYPAL_SECRET}
